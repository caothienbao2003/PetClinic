@page
@model PetClinic.Pages.Staff.CageManagement.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Cage</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Cage.CageId" />
            <div class="form-group">
                <label asp-for="Cage.CageStatus" class="control-label"></label>
                <select asp-for="Cage.CageStatus" class="form-control" asp-items="Model.StatusOptions"></select>
                <span asp-validation-for="Cage.CageStatus" class="text-danger"></span>
            </div>
            @*<div class="form-group">
                <label asp-for="Cage.ActiveStatus" class="control-label"></label>
                <input asp-for="Cage.ActiveStatus" class="form-control" />
                <span asp-validation-for="Cage.ActiveStatus" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
