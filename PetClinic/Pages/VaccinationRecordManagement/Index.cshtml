@page
@model PetClinic.Pages.VaccinationRecordManagement.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VaccinationRecord[0].VaccinationDetails)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.VaccinationRecord) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VaccinationDetails.VaccinationDetailsId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.VaccinationRecordsId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.VaccinationRecordsId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.VaccinationRecordsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
